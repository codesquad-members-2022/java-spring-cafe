create table cafe_user (
                        userId varchar(255) primary key,
                        password varchar(255) not null,
                        name varchar(255),
                        email varchar(255)
)
