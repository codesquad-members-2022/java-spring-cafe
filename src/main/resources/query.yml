### article ####

INSERT_ARTICLE: >
  INSERT INTO article(writer, title, contents, created_date)
  VALUES (:writer, :title, :contents, :createdDate)

SELECT_ARTICLES: >
  SELECT * FROM article

SELECT_ARTICLE: >
  SELECT * FROM article WHERE article_id = :articleId

DELETE_ARTICLES: >
  DELETE FROM article

UPDATE_ARTICLE: >
  UPDATE article SET title = :title, contents = :contents WHERE article_id = :articleId

DELETE_ARTICLE: >
  DELETE FROM article WHERE article_id = :articleId

### user ###

COUNT_USER: >
  SELECT COUNT(*) FROM `user` WHERE user_id = :userId

INSERT_USER: >
  INSERT INTO `user`(user_id, password, `name`, email)
  VALUES (:userId, :password, :name, :email)

UPDATE_USER: >
  UPDATE `user` SET `name` = :name, email = :email WHERE user_id = :userId

SELECT_USERS: >
  SELECT * FROM `user`

SELECT_USER: >
  SELECT * FROM `user` WHERE user_id = :userId

DELETE_USERS: >
  DELETE FROM `user`
