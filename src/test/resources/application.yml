spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:spring-cafe;mode=MYSQL;
    username: sa
    password:
  sql:
    init:
      platform: h2
      mode: always
      schema-locations: classpath:sql/schema-h2.sql